from scrapemm.secrets import configure_secrets, override_secret, update_config

configure_secrets(all_keys=True)
# update_config(firecrawl_url="http://scipio.mai.informatik.tu-darmstadt.de:3002/")
# override_secret("decodo_token")
